enum VideoTrackerConstants {
    static let updateProgressError = "❌ VastTracker update progress failed"
    static let emptyAdId = "❌ VastTracker empty ad id"
    static let trackAdStartError = "❌ VastTracker track ad start failed"
    static let trackAdCompleteError = "❌ VastTracker track ad complete failed"
    static let receiveTrackTypeError = "❌ VastTracker receive track type failed"
}
